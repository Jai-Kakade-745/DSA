//Binary search
//prerequisite

// Instead of checking every element (linear search), binary search always checks the middle element and eliminates 
// half the remaining elements each step.

// first it takes a middle element , 
//If the( Key > Middle element) then search for second half
// If (Key < Middle element) then search for first half 
// if ( Key = Middle element ) then return middle element
